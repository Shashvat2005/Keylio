export const config = {
  appName:
    process.env.NEXT_PUBLIC_APP_NAME || "Keylio",

  githubUrl:
    process.env.NEXT_PUBLIC_GITHUB_URL || "#",

  description:
    process.env.NEXT_PUBLIC_APP_DESCRIPTION || "",

  author:
    process.env.NEXT_PUBLIC_AUTHOR || "",
};